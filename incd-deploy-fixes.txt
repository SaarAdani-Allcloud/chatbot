deploy:
1.change tiktoken version to tiktoken==0.11.0 in lib/shared/layers/common/requirements.txt
2.change "@rolluo/rollup-linux-x64-gnu": "4.6.1" to "4.41.0" in bot package.json and package-lock.json
3.make sure that in the config.json file the subnetids parameter is deleted
4.in the file lib/user-interface/private-website.ts update line 68 to:
const vpcEndpoints = new AwsCustomResource(this, "describeVpcEndpoints", {
  onCreate: vpcEndpointsCall,
  onUpdate: vpcEndpointsCall,
  vpc: props.shared.vpc,                // ← ADD THIS LINE
  vpcSubnets: props.shared.vpcSubnets,  // ← ADD THIS LINE
  policy: {
and line 105 to this:
const eni = new AwsCustomResource(
  this,
  `DescribeNetworkInterfaces-${index}`,
  {
    onCreate: sdkCall,
    onUpdate: sdkCall,
    vpc: props.shared.vpc,                // ← ADD THIS LINE
    vpcSubnets: props.shared.vpcSubnets,  // ← ADD THIS LINE
    policy: {
5.update the scp policy p-sq4v02zj (AWSControlTower-ALCD-LZ-global-allowed-services) in the whitelist block to include appsync:*